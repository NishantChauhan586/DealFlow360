{
  "name": "dealflow360-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend/*"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend/user",
    "dev:user": "npm run dev --prefix frontend/user",
    "build": "npm run build --prefix frontend/user",
    "build:user": "npm run build --prefix frontend/user",
    "preview": "npm run preview --prefix frontend/user"
  }
}

